
var country = "CANADA";

if(["Canada", "USA"].includes(country)) {
  // North American phone verification.
} else {
  // just need it to be provided with at least 8 digits (including country code), 
     // not important what format.
}

